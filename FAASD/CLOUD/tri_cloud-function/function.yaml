provider:
  name: faas
  gateway: http://127.0.0.1:8080  # A DEFINIR

functions:
  tri:
    lang: python3
    handler: .
    image: <ton_dockerhub>/tri:latest # A DEFINIR
    annotations:
      schedule: "0 0 * * *"
